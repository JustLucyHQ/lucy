// Preload runs with contextIsolation. Nothing is exposed to the renderer in v1
// — Lucy runs as a normal web app inside the window. Add a contextBridge here
// later if the desktop build needs native capabilities (file dialogs, etc.).
