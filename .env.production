# Production-only, non-secret public env — loaded by `next build`, NOT `next dev`.
# (A GA measurement ID is public — it appears in the page source — so it's safe
# to commit here.) Cleared in the `desktop:build` script so the desktop /
# standalone app ships without analytics and stays local-first.

NEXT_PUBLIC_GA_ID=G-9S8FVHEPLV
